// Twitter uses the same share card as Open Graph. Re-export the sibling
// opengraph-image so there is a single source of truth for the design.
export { default, alt, size, contentType } from "./opengraph-image";
